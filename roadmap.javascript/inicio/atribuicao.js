// No código abaixo o sinal de = é lido como recebe
let valor = 2;

/*
    Todos os operadores de atribuiçãorder

    =
    += 
    -=
    *=
    /=
    %=
    **=
*/


/*
    Todos os operadores de Turno

    Operator	Example	    Same As
    <<=     	x <<= y	    x = x << y
    >>=     	x >>= y	    x = x >> y
    >>>=	    x >>>= y	x = x >>> y

*/


/*
    Operadores de Atribuição Lógica

    Operator	Example	    Same As
    &&=	        x &&= y	    x = x && (x = y)
    ||=	        x ||= y	    x = x || (x = y)
    ??=	        x ??= y	    x = x ?? (x = y)

*/